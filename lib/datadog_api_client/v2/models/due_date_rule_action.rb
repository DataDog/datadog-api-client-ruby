=begin
#Datadog API V2 Collection

#Collection of all Datadog Public endpoints.

The version of the OpenAPI document: 1.0
Contact: support@datadoghq.com
Generated by: https://github.com/DataDog/datadog-api-client-ruby/tree/master/.generator

 Unless explicitly stated otherwise all files in this repository are licensed under the Apache-2.0 License.
 This product includes software developed at Datadog (https://www.datadoghq.com/).
 Copyright 2020-Present Datadog, Inc.

=end

require 'date'
require 'time'

module DatadogAPIClient::V2
  # The action to take when the due date rule matches a finding.
  class DueDateRuleAction
    include BaseGenericModel

    # A list of severity-to-due-date mappings. Each severity may appear at most once.
    attr_reader :due_days_per_severity

    # The reference point from which the due date is calculated. When `fix_available` is selected but not applicable to the finding type, `first_seen` is used instead.
    attr_reader :due_from

    # An optional description providing more context for the due date assignment.
    attr_reader :reason_description

    attr_accessor :additional_properties

    # Attribute mapping from ruby-style variable name to JSON key.
    # @!visibility private
    def self.attribute_map
      {
        :'due_days_per_severity' => :'due_days_per_severity',
        :'due_from' => :'due_from',
        :'reason_description' => :'reason_description'
      }
    end

    # Attribute type mapping.
    # @!visibility private
    def self.openapi_types
      {
        :'due_days_per_severity' => :'Array<DueDatePerSeverityItem>',
        :'due_from' => :'DueDateFrom',
        :'reason_description' => :'String'
      }
    end

    # Initializes the object
    # @param attributes [Hash] Model attributes in the form of hash
    # @!visibility private
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `DatadogAPIClient::V2::DueDateRuleAction` initialize method"
      end

      self.additional_properties = {}
      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          self.additional_properties[k.to_sym] = v
        else
          h[k.to_sym] = v
        end
      }

      if attributes.key?(:'due_days_per_severity')
        if (value = attributes[:'due_days_per_severity']).is_a?(Array)
          self.due_days_per_severity = value
        end
      end

      if attributes.key?(:'due_from')
        self.due_from = attributes[:'due_from']
      end

      if attributes.key?(:'reason_description')
        self.reason_description = attributes[:'reason_description']
      end
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    # @!visibility private
    def valid?
      return false if @due_days_per_severity.nil?
      return false if @due_from.nil?
      return false if !@reason_description.nil? && @reason_description.to_s.length > 20000
      true
    end

    # Custom attribute writer method with validation
    # @param due_days_per_severity [Object] Object to be assigned
    # @!visibility private
    def due_days_per_severity=(due_days_per_severity)
      if due_days_per_severity.nil?
        fail ArgumentError, 'invalid value for "due_days_per_severity", due_days_per_severity cannot be nil.'
      end
      @due_days_per_severity = due_days_per_severity
    end

    # Custom attribute writer method with validation
    # @param due_from [Object] Object to be assigned
    # @!visibility private
    def due_from=(due_from)
      if due_from.nil?
        fail ArgumentError, 'invalid value for "due_from", due_from cannot be nil.'
      end
      @due_from = due_from
    end

    # Custom attribute writer method with validation
    # @param reason_description [Object] Object to be assigned
    # @!visibility private
    def reason_description=(reason_description)
      if !reason_description.nil? && reason_description.to_s.length > 20000
        fail ArgumentError, 'invalid value for "reason_description", the character length must be smaller than or equal to 20000.'
      end
      @reason_description = reason_description
    end

    # Returns the object in the form of hash, with additionalProperties support.
    # @return [Hash] Returns the object in the form of hash
    # @!visibility private
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      self.additional_properties.each_pair do |attr, value|
        hash[attr] = value
      end
      hash
    end

    # Checks equality by comparing each attribute.
    # @param o [Object] Object to be compared
    # @!visibility private
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          due_days_per_severity == o.due_days_per_severity &&
          due_from == o.due_from &&
          reason_description == o.reason_description &&
          additional_properties == o.additional_properties
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    # @!visibility private
    def hash
      [due_days_per_severity, due_from, reason_description, additional_properties].hash
    end
  end
end
