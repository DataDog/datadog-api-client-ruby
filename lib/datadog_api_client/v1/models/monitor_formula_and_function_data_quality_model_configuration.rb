=begin
#Datadog API V1 Collection

#Collection of all Datadog Public endpoints.

The version of the OpenAPI document: 1.0
Contact: support@datadoghq.com
Generated by: https://github.com/DataDog/datadog-api-client-ruby/tree/master/.generator

 Unless explicitly stated otherwise all files in this repository are licensed under the Apache-2.0 License.
 This product includes software developed at Datadog (https://www.datadoghq.com/).
 Copyright 2020-Present Datadog, Inc.

=end

require 'date'
require 'time'

module DatadogAPIClient::V1
  # Tuning options for the anomaly detection model used by the monitor.
  class MonitorFormulaAndFunctionDataQualityModelConfiguration
    include BaseGenericModel

    # Number of days after which an open alert is automatically resolved.
    # When unset, alerts stay open until the measure returns within bounds.
    attr_reader :auto_resolve_days

    # Whether to alert when the measure stops changing entirely.
    # Defaults to `true`.
    attr_accessor :enable_flatline_detection

    # Function applied to the measure before it is compared against the predicted bounds.
    attr_accessor :function

    # Minimum distance between the predicted value and the lower bound. Widening the
    # lower bound to at least this size suppresses alerts on small downward deviations.
    # When unset, no minimum is enforced.
    attr_accessor :min_lower_bound_size

    # Minimum distance between the predicted value and the upper bound. Widening the
    # upper bound to at least this size suppresses alerts on small upward deviations.
    # When unset, no minimum is enforced.
    attr_accessor :min_upper_bound_size

    # Restricts which predicted bound the monitor alerts on. `UPPER_ONLY` alerts only when
    # the measure rises above the upper bound, `LOWER_ONLY` only when it falls below the
    # lower bound. When unset, the monitor alerts on both.
    attr_accessor :model_bounds_override

    attr_accessor :additional_properties

    # Attribute mapping from ruby-style variable name to JSON key.
    # @!visibility private
    def self.attribute_map
      {
        :'auto_resolve_days' => :'auto_resolve_days',
        :'enable_flatline_detection' => :'enable_flatline_detection',
        :'function' => :'function',
        :'min_lower_bound_size' => :'min_lower_bound_size',
        :'min_upper_bound_size' => :'min_upper_bound_size',
        :'model_bounds_override' => :'model_bounds_override'
      }
    end

    # Attribute type mapping.
    # @!visibility private
    def self.openapi_types
      {
        :'auto_resolve_days' => :'Integer',
        :'enable_flatline_detection' => :'Boolean',
        :'function' => :'MonitorFormulaAndFunctionDataQualityDiffFunction',
        :'min_lower_bound_size' => :'Float',
        :'min_upper_bound_size' => :'Float',
        :'model_bounds_override' => :'MonitorFormulaAndFunctionDataQualityModelBoundsOverride'
      }
    end

    # Initializes the object
    # @param attributes [Hash] Model attributes in the form of hash
    # @!visibility private
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `DatadogAPIClient::V1::MonitorFormulaAndFunctionDataQualityModelConfiguration` initialize method"
      end

      self.additional_properties = {}
      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          self.additional_properties[k.to_sym] = v
        else
          h[k.to_sym] = v
        end
      }

      if attributes.key?(:'auto_resolve_days')
        self.auto_resolve_days = attributes[:'auto_resolve_days']
      end

      if attributes.key?(:'enable_flatline_detection')
        self.enable_flatline_detection = attributes[:'enable_flatline_detection']
      end

      if attributes.key?(:'function')
        self.function = attributes[:'function']
      end

      if attributes.key?(:'min_lower_bound_size')
        self.min_lower_bound_size = attributes[:'min_lower_bound_size']
      end

      if attributes.key?(:'min_upper_bound_size')
        self.min_upper_bound_size = attributes[:'min_upper_bound_size']
      end

      if attributes.key?(:'model_bounds_override')
        self.model_bounds_override = attributes[:'model_bounds_override']
      end
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    # @!visibility private
    def valid?
      return false if !@auto_resolve_days.nil? && @auto_resolve_days > 365
      return false if !@auto_resolve_days.nil? && @auto_resolve_days < 1
      true
    end

    # Custom attribute writer method with validation
    # @param auto_resolve_days [Object] Object to be assigned
    # @!visibility private
    def auto_resolve_days=(auto_resolve_days)
      if !auto_resolve_days.nil? && auto_resolve_days > 365
        fail ArgumentError, 'invalid value for "auto_resolve_days", must be smaller than or equal to 365.'
      end
      if !auto_resolve_days.nil? && auto_resolve_days < 1
        fail ArgumentError, 'invalid value for "auto_resolve_days", must be greater than or equal to 1.'
      end
      @auto_resolve_days = auto_resolve_days
    end

    # Returns the object in the form of hash, with additionalProperties support.
    # @return [Hash] Returns the object in the form of hash
    # @!visibility private
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      self.additional_properties.each_pair do |attr, value|
        hash[attr] = value
      end
      hash
    end

    # Checks equality by comparing each attribute.
    # @param o [Object] Object to be compared
    # @!visibility private
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          auto_resolve_days == o.auto_resolve_days &&
          enable_flatline_detection == o.enable_flatline_detection &&
          function == o.function &&
          min_lower_bound_size == o.min_lower_bound_size &&
          min_upper_bound_size == o.min_upper_bound_size &&
          model_bounds_override == o.model_bounds_override &&
          additional_properties == o.additional_properties
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    # @!visibility private
    def hash
      [auto_resolve_days, enable_flatline_detection, function, min_lower_bound_size, min_upper_bound_size, model_bounds_override, additional_properties].hash
    end
  end
end
